server {
	listen 80;
	server_name default_server;

	location / {
		include proxy_params;
		proxy_pass http://unix:/tmp/segments.sock;
	}
	location /static {
		alias /home/target_user/project/segments/static/;
		autoindex on;
	}
}
